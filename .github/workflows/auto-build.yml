name: AutoBuild

on:
  workflow_dispatch:
  schedule:
    - cron: '0 20 * * *'

permissions: write-all

jobs:
  buildForWindows: 
    name: Build Windows🪟🪟🪟
    runs-on: ubuntu-latest
    steps:
      - name: Check Out
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Cache Library
        uses: actions/cache@v4
        with: 
          path: Unity/ECO/Library
          key: Library-${{ hashFiles('Unity/ECO/Assets/**', 'Unity/ECO/Packages/**', 'Unity/ECO/ProjectSettings/**') }}
          restore-keys: |
            Library-

      - name: Unity Build
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows64
          projectPath: Unity/ECO
          
      - name: Upload Build File
        uses: actions/upload-artifact@v4
        with:
          name: Build-Windows
          path: build/StandaloneWindows64
